import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'War Stories — GIA ANH SOP' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
