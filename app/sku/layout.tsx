import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SKU Tracking — GIA ANH SOP' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
