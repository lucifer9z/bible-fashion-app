import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Cửa hàng — BibleFashion' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
